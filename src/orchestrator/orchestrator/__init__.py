"""Orchestrator package.

Provides supervision of external services (capture, control, harvester, session-manager, detection).
The session manager logic now lives in the standalone 'nexus-session-manager' package.
"""
