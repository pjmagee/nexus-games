{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "sync dependencies",
            "type": "shell",
            "command": "uv sync --group dev",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": "build",
            "problemMatcher": [],
            "presentation": {
                "reveal": "always",
                "panel": "dedicated"
            }
        },
        {
            "label": "run inference service",
            "type": "shell",
            "command": "uv run python -m detection.service",
            "options": {
                "cwd": "${workspaceFolder}",
                "env": {
                    "NEXUS_BASE_DIR": "${workspaceFolder}/../.."
                }
            },
            "dependsOn": ["sync dependencies"],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        },
        {
            "label": "run inference (backfill once)",
            "type": "shell",
            "command": "uv run python -m detection.service",
            "options": {
                "cwd": "${workspaceFolder}",
                "env": {
                    "DETECTION_RUN_ONCE": "1"
                }
            },
            "dependsOn": ["sync dependencies"],
            "problemMatcher": []
        }
    ]
}
